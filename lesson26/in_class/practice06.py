"""
[难度: ⭐⭐⭐]
[所属知识点: MLP 前向传播 / np.dot + 激活函数]
[预计完成时间: 15 分钟]

垃圾邮件二分类器:输入 4 维词频特征,隐藏层 5 个神经元用
ReLU,输出层 2 维用 Softmax。网络结构为 input(4) →
hidden(5) → output(2)。用 np.random.seed(42) 固定权重,
实现前向传播,返回最终 softmax 概率。

示例:
    >>> np.random.seed(42)
    >>> x = np.array([[1.0, 2.0, 3.0, 4.0]])
    >>> out = mlp_forward(x)
    >>> out.shape
    (1, 2)
    >>> np.isclose(out.sum(), 1.0)
    True
"""

# ======================
# 学员代码区(以 pass 作为占位符)
# ======================
pass

# ======================
# 测试区(教师可复制到终端验证)
# ======================
if __name__ == '__main__':
    # 测试 1: 单样本输入 shape(1,4)
    # 测试 2: 批输入 shape(3,4),概率行和验证
    pass
