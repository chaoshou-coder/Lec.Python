"""
[难度: ⭐⭐]
[所属知识点: 激活函数 Sigmoid / 数值稳定性]
[预计完成时间: 10 分钟]

银行贷款审批系统会把客户的综合评分 x 映射为"通过概率"。
sigmoid(x) = 1 / (1 + exp(-x))。当 x 为很大的负数时,
exp(-x) 会溢出为 inf,导致结果为 NaN。请实现一个数值稳定
的 sigmoid 函数:当 x >= 0 时用原始公式,当 x < 0 时等价
变形为 exp(x) / (1 + exp(x)) 以避免溢出。

示例:
    >>> stable_sigmoid(np.array([0, 2, -5]))
    array([0.5       , 0.88079708, 0.00669285])
    >>> stable_sigmoid(np.array([-1000]))
    array([0.])
"""

# ======================
# 学员代码区(以 pass 作为占位符)
# ======================
pass

# ======================
# 测试区(教师可复制到终端验证)
# ======================
if __name__ == '__main__':
    # 测试 1: 常规输入 [0, 2, -5, 100, -100]
    # 测试 2: 极端值 [-1000, 1000] 不应出现 NaN
    pass
