"""
[难度: ⭐⭐]
[所属知识点: 激活函数 Tanh / ReLU / 向量化]
[预计完成时间: 10 分钟]

在神经网络的隐藏层中,Tanh 和 ReLU 是两种常用的激活函数。
Tanh(x) = (exp(x) - exp(-x)) / (exp(x) + exp(-x)),
ReLU(x) = max(0, x)。请用 NumPy 的向量化操作同时实现
这两个函数,并观察 ReLU 对负值"死亡"的现象。

示例:
    >>> tanh(np.array([-1, 0, 1]))
    array([-0.76159416,  0.        ,  0.76159416])
    >>> relu(np.array([-2, 0, 3]))
    array([0, 0, 3])
"""

# ======================
# 学员代码区(以 pass 作为占位符)
# ======================
pass

# ======================
# 测试区(教师可复制到终端验证)
# ======================
if __name__ == '__main__':
    # 测试 1: x = np.linspace(-3, 3, 7)
    # 测试 2: 边界值 0 和全负数组
    pass
