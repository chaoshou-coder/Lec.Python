"""
[难度: ⭐⭐⭐]
[所属知识点: 损失函数 MSE / 梯度]
[预计完成时间: 15 分钟]

房价预测任务中,真实房价 y_true 和模型预测 y_pred 都是
一维数组。MSE 损失 = mean((y_true - y_pred) ^ 2),
损失对 y_pred 的梯度 = 2 * (y_pred - y_true) / n。
请同时实现 MSE 损失与梯度计算,返回 (loss, grad)。

示例:
    >>> compute_mse(np.array([3.0, 5.0]),
    ...             np.array([2.5, 5.5]))
    (0.25, array([-0.5, 0.5]))
"""

# ======================
# 学员代码区(以 pass 作为占位符)
# ======================
pass

# ======================
# 测试区(教师可复制到终端验证)
# ======================
if __name__ == '__main__':
    # 测试 1: 常规 2 元素数组
    # 测试 2: 全零预测(零值边界)
    pass
