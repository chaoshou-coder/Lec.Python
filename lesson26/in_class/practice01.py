"""
[难度: ⭐]
[所属知识点: 感知机 / 前向传播]
[预计完成时间: 5 分钟]

智能门锁的"与门"逻辑:只有当指纹识别通过(输入 1)且密码验证
通过(输入 1)时,门锁才会打开(输出 1)。现给定一个感知机,权重
w1=0.6、w2=0.6、偏置 b=-1.0,激活函数为阶跃函数(step)。
请用 NumPy 实现感知机的前向传播与阶跃函数,并验证 AND 门
的完整真值表。

示例:
    >>> perceptron(np.array([0, 0]))
    0
    >>> perceptron(np.array([1, 1]))
    1
"""

# ======================
# 学员代码区(以 pass 作为占位符)
# ======================
pass

# ======================
# 测试区(教师可复制到终端验证)
# ======================
if __name__ == '__main__':
    # 测试 1: 验证 AND 门真值表四种输入
    # 测试 2: 验证全 1 输入输出为 1
    pass
