"""
[难度: ⭐⭐⭐⭐]
[所属知识点: sorted(key=lambda x: ...)]
[预计完成时间: 20 分钟]

题目描述:
    请用 sorted 配合 lambda 对以下字典列表按 'age' 字段
    进行升序排序,并打印排序后的结果。

示例:
    >>> students = [{'name': '张三', 'age': 20}, ...]
    >>> sorted(students, key=lambda x: x['age'])
    [{'name': '王五', 'age': 18}, ...]
"""

# ======================
# 学员代码区(以 pass 作为占位符)
# ======================
pass

# ======================
# 测试区(教师可复制到终端验证)
# ======================
if __name__ == '__main__':
    # 测试 1: 对字典列表按年龄升序排序
    # 测试 2: 按年龄降序排序(提示:reverse=True)
    # 测试 3: 边界 — 只有 1 个学生
    pass
