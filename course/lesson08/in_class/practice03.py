"""
[难度: ⭐⭐⭐]
[所属知识点: 字符串split()]
[预计完成时间: 15 分钟]

题目描述:
    输入一行 CSV 数据(如 "张三,20,成都"),用 split 拆成列表,并打印结果。

示例:
    >>> line = "张三,20,成都"
    输出: ['张三', '20', '成都']

"""

# ======================
# 学员代码区(以 pass 作为占位符)
# ======================
line = input("请输入一行 CSV 数据(逗号分隔): ")
parts = line.split(",")
print("拆分结果:", parts)

# ======================
# 测试区(教师可复制到终端验证)
# ======================
if __name__ == '__main__':
    # 测试 1: 正常 CSV
    print(f"测试1: {'张三,20,成都'.split(',')}")

    # 测试 2: 单个值
    print(f"测试2: {'hello'.split(',')}")

    # 测试 3: 包含空值
    print(f"测试3: {'a,,c'.split(',')}")
