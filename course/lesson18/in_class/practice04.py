"""
[难度: ⭐⭐]
[所属知识点: ML 流程排序]
[预计完成时间: 10 分钟]

题目: 写一个函数 check_ml_order(steps),
检查是否符合标准顺序(数据收集→数据清洗→特征工程→模型训练→模型评估)。

标准顺序: ["数据收集","数据清洗","特征工程","模型训练","模型评估"]

规则:
1. 按标准顺序比对,返回第一个不匹配位置对应的步骤名
2. 若 steps 是合法前缀,返回标准顺序中下一步
3. 若全部匹配则返回"已完成"
4. 若某一步不在标准顺序中,返回"步骤错误:XXX"

示例:
    >>> check_ml_order(["数据清洗"])
    '数据收集'
    >>> check_ml_order(["数据收集","数据清洗","特征工程","模型训练","模型评估"])
    '已完成'
"""

# ======================
# 学员代码区(以 pass 作为占位符)
# ======================
pass

# ======================
# 测试区(教师可复制到终端验证)
# ======================
if __name__ == '__main__':
    # 测试 1: 第一步就错,应返回标准顺序首步"数据收集"
    print(check_ml_order(["数据清洗"]))

    # 测试 2: 完整且顺序正确,应返回"已完成"
    print(check_ml_order(
        ["数据收集","数据清洗","特征工程","模型训练","模型评估"]
    ))

    # 测试 3: 合法前缀,返回下一步"特征工程"
    print(check_ml_order(["数据收集","数据清洗"]))

    # 测试 4: 中间缺步骤,应返回"数据清洗"
    print(check_ml_order(["数据收集","特征工程"]))

    # 测试 5: 包含非法步骤名
    print(check_ml_order(["数据收集"]))
