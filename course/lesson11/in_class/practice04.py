"""
[难度: ⭐⭐⭐]
[所属知识点: 广播(int + array)]
[预计完成时间: 15 分钟]

题目描述:
    NumPy 广播(Broadcasting)允许不同形状的数组进行运算。

    请观察以下广播现象:
      1. 标量 + 数组: 5 + [1, 2, 3]
      2. 标量 * 数组: 2 * [10, 20, 30]
      3. 一维 + 二维: [1, 2, 3] + [[10], [20]]

    广播规则: 把小数组"拉伸"到大数组的形状再计算。

示例:
    >>> a = np.array([1, 2, 3])
    >>> print(5 + a)
    [6 7 8]
    >>> b = np.array([[10], [20]])
    >>> print(a + b)
    [[11 12 13]
     [21 22 23]]
"""

# ======================
# 学员代码区(以 pass 作为占位符)
# ======================

import numpy as np

a = np.array([1, 2, 3])
b = np.array([[10], [20]])

# 任务 1: 标量 + 数组
result1 = None  # 替换为 5 + a

# 任务 2: 标量 * 数组
c = np.array([10, 20, 30])
result2 = None  # 替换为 2 * c

# 任务 3: 一维 + 二维
result3 = None  # 替换为 a + b

# ======================
# 测试区(教师可复制到终端验证)
# ======================
if __name__ == '__main__':
    # 测试 1: 标量加
    print(f"5 + a = {result1}")
    # 测试 2: 标量乘
    print(f"2 * c = {result2}")
    # 测试 3: 一维加二维
    print(f"a + b =\n{result3}")
