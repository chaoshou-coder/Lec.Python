"""
[难度: ⭐⭐⭐]
[所属知识点: lambda + sorted 多关键字排序]
[预计完成时间: 15 分钟]

题目描述:
    给定一组学生信息(姓名, 年龄, 成绩),请使用 lambda 和
    sorted 实现多关键字排序:
      1. 按成绩降序排列
      2. 成绩相同时,按年龄升序排列

    lambda 匿名函数语法: lambda 参数: 返回值

示例:
    >>> students = [
    ...     ("张三", 20, 85),
    ...     ("李四", 19, 90),
    ...     ("王五", 21, 85),
    ...     ("赵六", 20, 92),
    ... ]
    >>> sort_students(students)
    [('赵六', 20, 92), ('李四', 19, 90),
     ('张三', 20, 85), ('王五', 21, 85)]
"""

# ======================
# 学员代码区(以 pass 作为占位符)
# ======================

def sort_students(students):
    """按成绩降序、年龄升序排序学生列表"""
    # 提示: sorted(students, key=lambda s: (-s[2], s[1]))
    pass

# ======================
# 测试区(教师可复制到终端验证)
# ======================
if __name__ == '__main__':
    students = [
        ("张三", 20, 85),
        ("李四", 19, 90),
        ("王五", 21, 85),
        ("赵六", 20, 92),
    ]
    # 测试 1: 多关键字排序
    result = sort_students(students)
    for s in result:
        print(f"{s[0]}: 年龄={s[1]}, 成绩={s[2]}")
    # 测试 2: 空列表
    print(f"空列表: {sort_students([])}")
