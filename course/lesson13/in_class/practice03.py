"""
[难度: ⭐⭐⭐⭐]
[所属知识点: sorted(key=lambda x: ...)]
[预计完成时间: 20 分钟]

题目描述:
    请用 sorted 配合 lambda 对元组列表
    [(1, 2), (3, 1), (2, 3)]
    按照每个元组的第二个元素进行升序排序。

示例:
    >>> sorted([(1, 2), (3, 1), (2, 3)],
    ...        key=lambda x: x[1])
    [(3, 1), (1, 2), (2, 3)]
"""

# ======================
# 学员代码区(以 pass 作为占位符)
# ======================
pass

# ======================
# 测试区(教师可复制到终端验证)
# ======================
if __name__ == '__main__':
    # 测试 1: 按第二个元素升序
    # 测试 2: 按第二个元素降序
    # 测试 3: 边界 — 空列表
    pass
