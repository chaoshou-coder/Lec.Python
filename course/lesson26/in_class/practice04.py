"""
[难度: ⭐⭐]
[所属知识点: Softmax / 数值稳定性]
[预计完成时间: 10 分钟]

一个图像分类网络输出三张类别(logits)分数,对应"猫/狗/鸟"。
softmax 把 logits 转为概率分布: softmax(x_i) =
exp(x_i - max(x)) / sum(exp(x_j - max(x)))。减最大值
是数值稳定技巧,防止 exp 溢出。请实现数值稳定的 softmax。

示例:
    >>> softmax(np.array([2.0, 1.0, 0.1]))
    array([0.65900114, 0.24243297, 0.09856589])
    >>> softmax(np.array([1000, 1000, 1000]))
    array([0.33333333, 0.33333333, 0.33333333])
"""

# ======================
# 学员代码区(以 pass 作为占位符)
# ======================
pass

# ======================
# 测试区(教师可复制到终端验证)
# ======================
if __name__ == '__main__':
    # 测试 1: 普通 logits [2.0, 1.0, 0.1],概率之和应为 1
    # 测试 2: 大且相等 logits [1000, 1000, 1000],应等分
    pass
