"""
[难度: ⭐⭐⭐⭐]
[所属知识点: 多值返回 + 默认参数综合]
[预计完成时间: 20 分钟]

题目描述:
    请定义一个名为 stats 的函数,接收三个参数:
    a、b、c,其中 c 的默认值为 0。

    函数同时返回三个值(元组):
    - 三个数的和
    - 三个数的平均值(保留两位小数)
    - 三个数中的最大值

    定义完成后,分别测试:
    1) 传入三个参数;
    2) 只传两个参数,使用 c 的默认值。

示例:
    >>> s, avg, mx = stats(10, 20, 30)
    >>> print(s, avg, mx)
    60 20.0 30
    >>> s, avg, mx = stats(10, 20)
    >>> print(s, avg, mx)
    30 15.0 20
"""

# ======================
# 学员代码区(以 pass 作为占位符)
# ======================
pass

# ======================
# 测试区(教师可复制到终端验证)
# ======================
if __name__ == '__main__':
    # 测试 1: 三个参数
    # s, avg, mx = stats(10, 20, 30)
    # print(s, avg, mx)
    # 测试 2: 两个参数,c 默认 0
    # s, avg, mx = stats(10, 20)
    # print(s, avg, mx)
    # 测试 3: 包含负数
    # s, avg, mx = stats(-5, 5, 0)
    # print(s, avg, mx)
    pass
