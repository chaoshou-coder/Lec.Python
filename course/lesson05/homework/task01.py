"""
[难度: ⭐⭐⭐]
[所属知识点: 函数封装 / BMI 计算]
[预计完成时间: 15 分钟]

题目描述:
    请定义一个名为 calc_bmi 的函数,接收两个参数:
    weight(体重,单位 kg)、height(身高,单位 m)。

    函数内部按公式计算 BMI:
        BMI = weight / (height * height)

    并通过 return 返回结果(保留两位小数)。

    定义完成后,调用函数计算:体重 70kg、身高 1.75m,
    并打印结果。

示例:
    >>> bmi = calc_bmi(70, 1.75)
    >>> print(bmi)
    22.86
"""

# ======================
# 学员代码区(以 pass 作为占位符)
# ======================
pass

# ======================
# 测试区(教师可复制到终端验证)
# ======================
if __name__ == '__main__':
    # 测试 1: 正常数据 70kg / 1.75m
    # print(calc_bmi(70, 1.75))
    # 测试 2: 边界,较轻体重
    # print(calc_bmi(50, 1.80))
    # 测试 3: 边界,较重体重
    # print(calc_bmi(100, 1.70))
    pass
