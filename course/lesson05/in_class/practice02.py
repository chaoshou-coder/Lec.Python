"""
[难度: ⭐⭐]
[所属知识点: def + return / 有参有返]
[预计完成时间: 10 分钟]

题目描述:
    请定义一个名为 add 的函数,接收两个参数 a 和 b,
    计算它们的和,并通过 return 把结果返回。
    函数定义完成后,调用 add(3, 5),把返回值存到
    变量 result,再打印 result。

示例:
    >>> result = add(3, 5)
    >>> print(result)
    8
"""

# ======================
# 学员代码区(以 pass 作为占位符)
# ======================
pass

# ======================
# 测试区(教师可复制到终端验证)
# ======================
if __name__ == '__main__':
    # 测试 1: add(3, 5) 应返回 8
    # result = add(3, 5)
    # print(result)
    # 测试 2: add(-2, 7) 应返回 5
    # print(add(-2, 7))
    # 测试 3: add(0, 0) 应返回 0(边界)
    # print(add(0, 0))
    pass
