"""
[难度: ⭐⭐⭐]
[所属知识点: 多值返回(元组解包)]
[预计完成时间: 15 分钟]

题目描述:
    请定义一个名为 calculate 的函数,接收两个参数
    a 和 b,同时返回三个值:a + b(和)、a - b(差)、
    a * b(积)。

    函数定义完成后,用三个变量接收返回值(元组解包),
    再分别打印这三个变量。

示例:
    >>> s, d, p = calculate(10, 3)
    >>> print(s, d, p)
    13 7 30
"""

# ======================
# 学员代码区(以 pass 作为占位符)
# ======================
pass

# ======================
# 测试区(教师可复制到终端验证)
# ======================
if __name__ == '__main__':
    # 测试 1: 正常正整数
    # s, d, p = calculate(10, 3)
    # print(s, d, p)
    # 测试 2: 包含 0(边界)
    # s, d, p = calculate(5, 0)
    # print(s, d, p)
    # 测试 3: 负数
    # s, d, p = calculate(-4, 2)
    # print(s, d, p)
    pass
