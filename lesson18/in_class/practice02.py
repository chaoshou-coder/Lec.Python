"""
[难度: ⭐⭐]
[所属知识点: 过拟合判断]
[预计完成时间: 10 分钟]

给定两个模型在训练集和测试集上的准确率,写一个函数
`detect_overfit(train_scores, test_scores)`,返回每个模型的状态:
"过拟合"、"正常" 或 "欠拟合"。

判断规则:
- 训练准确率 > 95% 且测试准确率 < 70% → "过拟合"
- 训练准确率 < 70% 且测试准确率 < 70% → "欠拟合"
- 其他情况 → "正常"

参数 train_scores 和 test_scores 为长度相同的列表,
逐元素判断后返回结果列表。

示例:
    >>> detect_overfit([0.99, 0.65, 0.88],
    ...                [0.60, 0.62, 0.85])
    ['过拟合', '欠拟合', '正常']
"""

# ======================
# 学员代码区(以 pass 作为占位符)
# ======================
pass

# ======================
# 测试区(教师可复制到终端验证)
# ======================
if __name__ == '__main__':
    # 测试 1: 三种情况各一个,验证基本分类正确
    # result1 = detect_overfit([0.99, 0.65, 0.88],
    #                          [0.60, 0.62, 0.85])
    # print(result1)
    # 应输出: ['过拟合', '欠拟合', '正常']
    #
    # 测试 2: 过拟合 + 欠拟合的边界情况
    # result2 = detect_overfit([0.97, 0.50],
    #                          [0.90, 0.45])
    # print(result2)
    # 应输出: ['正常', '欠拟合']
    #
    # 测试 3: 空列表,返回空列表(边界情况)
    # result3 = detect_overfit([], [])
    # print(result3)
    # 应输出: []
    pass
