"""
[难度: ⭐⭐]
[所属知识点: 梯度提升残差拟合直觉]
[预计完成时间: 10 分钟]

题目: 模拟梯度提升中"残差拟合"的核心直觉。
这是一个纯 numpy 计算题,不涉及 sklearn。

给定:
  y_true = [10, 20, 30, 40]
  初始预测(均值) = 25
  弱模型对残差的预测 r_hat = [-3, 7, -5, 1]
  学习率 eta = 0.1

请计算:
  (1) 残差 r = y_true - 初始预测
  (2) 第一轮修正后的新预测
      y_new = 初始预测 + eta * r_hat
  (3) 计算修正前后与真实值的 MSE,
      观察 MSE 是否下降

示例:
    >>> # 运行后输出类似:
    >>> # 残差 r: [-15  -5   5  15]
    >>> # 新预测: [24.7 25.7 24.5 25.1]
    >>> # 修正前 MSE: 125.0
    >>> # 修正后 MSE: 119.5725
"""

import numpy as np

# ======================
# 学员代码区(以 pass 作为占位符)
# ======================
# 1. 定义 y_true, y_pred, r_hat, eta
# 2. 计算残差 r
# 3. 计算新预测 y_new
# 4. 计算修正前后的 MSE 并输出
pass

# ======================
# 测试区(教师可复制到终端验证)
# ======================
if __name__ == '__main__':
    # 测试 1: 残差计算正确(应为 [-15,-5,5,15])
    # 测试 2: 修正后 MSE 应小于修正前
    pass
