"""
[难度: ⭐⭐]
[所属知识点: SVC 对特征尺度敏感]
[预计完成时间: 10 分钟]

题目: 构造一个特征尺度差异很大的数据集:
特征 1 的取值范围是 [0, 1],
特征 2 的取值范围是 [0, 1000]。
共生成 200 条样本,二分类标签由两个特征加权和
大于阈值决定。
用以下两种方式训练 SVC,对比准确率差异:
  (a) 无标准化,直接训练
  (b) 先用 StandardScaler 标准化,再训练
输出两种情况的测试集准确率。

提示: 在注释中解释为什么 SVM 需要特征缩放?
(因为 SVM 基于距离度量,尺度大的特征会主导
间隔计算,导致模型忽略小尺度特征的信息。)

示例:
    >>> # 运行后输出类似:
    >>> # 无标准化准确率: 0.65
    >>> # 标准化后准确率: 0.92
"""

import numpy as np
from sklearn.svm import SVC
from sklearn.preprocessing import StandardScaler
from sklearn.model_selection import train_test_split

# ======================
# 学员代码区(以 pass 作为占位符)
# ======================
# 1. 用 np.random.rand(200, 2) 生成特征,
#    第 2 列乘以 1000
# 2. 构造标签: (特征1 + 0.001 * 特征2) > 0.5
# 3. 划分训练集/测试集, random_state=42
# 4. 方式 a: 直接 SVC() 训练,输出准确率
# 5. 方式 b: StandardScaler 后再 SVC(),
#    输出准确率
pass

# ======================
# 测试区(教师可复制到终端验证)
# ======================
if __name__ == '__main__':
    # 测试 1: 无标准化 — 准确率应明显偏低
    # 测试 2: 标准化后 — 准确率应显著提高
    pass
