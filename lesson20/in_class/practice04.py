"""
[难度: ⭐⭐⭐⭐]
[所属知识点: 类属性 vs 实例属性]
[预计完成时间: 20 分钟]

题目描述:
在 Animal 类中添加类属性 count,每创建一个实例 +1,
用于统计创建了多少只动物。注意类属性是所有实例共享的。

示例:
    >>> Animal.count
    0
    >>> d = Dog("旺财")
    >>> c = Cat("小花")
    >>> Animal.count
    2
"""

# ======================
# 学员代码区(以 pass 作为占位符)
# ======================
pass

# ======================
# 测试区(教师可复制到终端验证)
# ======================
if __name__ == '__main__':
    # 测试 1: 初始数量为 0
    print(f"初始动物数: {Animal.count}")

    # 测试 2: 创建一只
    d1 = Dog("旺财")
    print(f"创建 1 只后: {Animal.count}")

    # 测试 3: 创建多只
    c1 = Cat("小花")
    c2 = Cat("咪咪")
    a1 = Animal("某动物")
    print(f"创建 4 只后: {Animal.count}")

    # 测试 4: 通过实例访问类属性
    print(f"通过实例访问: {d1.count}")
